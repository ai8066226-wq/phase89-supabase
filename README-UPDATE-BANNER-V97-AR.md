# إصلاح تنبيه التحديث v97

السبب كان اختلاف الإصدار:
- `version.json` كان 2.8.6
- `pwa.js` كان 2.8.4

الإصلاح:
- توحيد الإصدار إلى 2.8.7.
- تحديث مرجع `pwa.js` إلى v97 في العميل والكابتن والخدمات.
- تحديث Service Worker إلى v97.
- تنبيه التحديث لا يبقى ظاهرًا: عند وجود تحديث حقيقي يظهر مرة واحدة فقط لكل إصدار لمدة 5.5 ثوانٍ، ولا يعيد تحميل الصفحة.

ارفع الملفات الموجودة في هذه الحزمة إلى جذر المستودع واستبدل القديمة.
